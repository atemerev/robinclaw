services:
  - type: web
    name: robinclaw
    runtime: python
    buildCommand: pip install .
    startCommand: uvicorn robinclaw.web.app:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: "3.11"
    plan: free
